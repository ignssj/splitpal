import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "center",
    paddingVertical: "5%",
    gap: 5,
  },
  row: {
    flex: 1,
    gap: 5,
  },
  title: {
    fontSize: 16,
  },
});

export default styles;
