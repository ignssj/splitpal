import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  container: {
    flex: 1,
    marginHorizontal: "8%",
  },
  row: {
    flex: 0.1,
    width: '90%',
    alignSelf: 'center',
    alignItems: "center",
  },
});

export default style;
